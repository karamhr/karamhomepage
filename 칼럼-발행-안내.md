# 노무칼럼 발행 안내

## 폴더 구조

```text
karamhomepage-work/
├─ columns.html                     노무칼럼 1페이지(최신 글)
├─ columns-page-2.html              노무칼럼 2페이지(자동 생성)
├─ columns-page-3.html              이후 페이지(필요 시 자동 생성)
├─ columns/                         실제 칼럼 상세 페이지(자동 생성)
│  ├─ column-01-contract.html
│  └─ column-07-dismissal-guide.html
├─ content/
│  └─ columns.json                  칼럼 원본 데이터(직접 편집)
├─ tools/
│  └─ generate_columns.py           생성 도구
└─ styles.css, theme-blue.css, column-detail.css
```

- 모든 개별 칼럼 HTML은 `columns/` 폴더에서만 관리합니다.
- `columns/` 안의 HTML과 `columns.html`은 생성 결과물이므로 직접 수정하지 않는 것을 권장합니다.
- 기존 최상위 `column-*.html` 주소는 더 이상 사용하지 않습니다.

## 새 칼럼 추가 방법

1. `content/columns.json` 파일을 엽니다.
2. `columns` 목록 맨 아래에 새 항목을 추가합니다.
   - 이전 항목 끝에 쉼표(`,`)가 있어야 합니다.
   - `id`는 기존 최대 번호보다 1 크게 입력합니다.
   - `slug`는 영문 소문자·숫자·하이픈만 사용합니다. 예: `dismissal-guide`
   - `body_html`에는 기존 글의 `<h2>`, `<p>`, `<ul>`, `<div class="point-box">` 형식을 사용합니다.
3. 프로젝트 최상위 폴더에서 아래 명령을 실행합니다.

```bash
python3 tools/generate_columns.py --project .
```

실행하면 다음이 자동으로 처리됩니다.
- `columns.html`에는 최신 칼럼 6편을 표시
- 7편 이상이면 `columns-page-2.html`, `columns-page-3.html` 등을 자동 생성
- 각 목록 페이지 하단에 이전·다음·페이지 번호 버튼 생성
- `columns/`에 새 상세 페이지 생성
- 모든 상세 글의 이전 글/다음 글 링크 갱신

## 새 칼럼 입력 예시

```json
{
  "id": 8,
  "slug": "dismissal-guide",
  "kicker": "WORKPLACE",
  "category": "인사 운영",
  "title": "권고사직을 검토할 때\n확인할 기준",
  "summary": "권고사직을 진행하기 전 확인할 절차와 유의사항을 정리합니다.",
  "reading_minutes": 5,
  "lead": "권고사직은 충분한 설명과 자발적 의사 확인이 핵심입니다.",
  "body_html": "<h2><span>POINT 01</span>사실관계를 먼저 확인합니다</h2><p>본문을 작성합니다.</p><div class=\"point-box\"><strong>실무 포인트</strong><p>핵심 내용을 작성합니다.</p></div>",
  "notice": "이 글은 일반적인 노무 정보를 제공하기 위한 자료입니다. 구체적인 사실관계에 따라 판단이 달라질 수 있습니다.",
  "cta_title": "인사 조치 전,\n절차부터 점검하세요.",
  "cta_text": "사업장 상황에 맞는 진행 절차를 함께 검토해 드립니다."
}
```

## 유의사항

- `content/columns.json` 수정 뒤에는 반드시 생성 명령을 실행해야 웹 페이지에 반영됩니다.
- JSON에서 문자열 내부의 큰따옴표는 `\"`로 작성해야 합니다.
- 칼럼 내용에 법령·제도 안내가 포함될 때에는 게시 전 최신 법령과 개별 사안 검토가 필요합니다.
